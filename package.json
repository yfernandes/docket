{
	"name": "docket",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "bun build ./src/cli.ts --target=bun --outfile=./task",
		"test": "bun test",
		"typecheck": "tsc -p scripts/tsconfig.json",
		"check": "bunx biome check .",
		"verify": "bun run build && bun run typecheck && bun run test && bun run check"
	},
	"devDependencies": {
		"@types/bun": "^1.3.14",
		"typescript": "^7.0.2"
	}
}

# Libs Backlog

Unscheduled library items.

- [ ] **[P2] Tree-shake shared UI bundle** — Current bundle includes all components even when only a subset is imported.
- [ ] **[P3] Type-safe environment config** — Replace raw `process.env` reads with a validated config schema.

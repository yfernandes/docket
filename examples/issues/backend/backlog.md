# Backend Backlog

Unscheduled backend items.

- [ ] **[P2] Rate limiting on public endpoints** — Add token-bucket rate limiting to `/api/products` and `/api/cart`.
- [ ] **[P2] Async job queue for order notifications** — Move order confirmation emails to a background job queue.
- [ ] **[P3] Pagination cursor stability** — Cursor-based pagination breaks when items are inserted mid-page.
